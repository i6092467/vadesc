# Data
data:
  data_name: flchain

training:
  # Training
  eval_frequency: 200

  # Architecture
  inp_shape: 8
  latent_dim: 4
  num_clusters: 2
  activation: "none"
  monte_carlo: 1
  learn_prior: True

  # Survival model
  weibull_shape: 1
  survival: True
  sample_surv: False